{
  "name": "devaseel-aura",
  "version": "0.0.4",
  "description": "Library for validating environment variables at runtime.",
  "main": "dist/index.js",
  "types": "dist/validate.d.ts",
  "author": "Aseel Al Rawahi",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.3",
    "zod": "^3.21.4"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tsc",
    "start": "npm run build && node dist/env.js"
  },
  "bin": {
    "validate": "~/dist/env.js"
  }
}
